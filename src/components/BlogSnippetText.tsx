import * as React from "react";
import styled from "styled-components";
import { Link } from "gatsby";

const BlogSnippetText = styled.p`
    font-size: 1.2em;
    color: #fff;
    font-weight: 100;
    font-family: neue-haas-grotesk-display, sans-serif;
    margin: 0.8em auto;
`;

export default BlogSnippetText
